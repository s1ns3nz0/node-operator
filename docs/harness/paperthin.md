# Pinned external skill contract

`harness/paperthin.lock` records the required Paperthin revision. Installation
success alone is not pin verification. Install or upgrade it only as an
explicit, reviewed task; reopening a session is recommended after verified
installation so the environment discovers the skills.
