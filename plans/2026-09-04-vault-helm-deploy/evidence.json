{
  "task_id": "2026-09-04-vault-helm-deploy",
  "status": "blocked_missing_eks_association_read_permission",
  "checks": [
    {
      "name": "private EKS prerequisite route",
      "status": "blocked",
      "result": "The target cluster is active and private-endpoint-only. This workstation has no target-cluster kubecontext, so it cannot verify the externally managed vault namespace or vault-tls Secret through the required private path."
    },
    {
      "name": "live deploy saved-plan gate",
      "status": "passed",
      "result": "A refresh-backed saved plan produced exactly one create, aws_eks_access_policy_association.vault_bootstrap_cluster_admin[0], with zero changes and zero deletes. Plan JSON SHA-256: d93bb1575b2859c31f01036d5096ad872efa96b869bfeb6eb56b98526c64e22a."
    },
    {
      "name": "approved deploy-plan apply",
      "status": "blocked",
      "result": "The exact saved plan stopped before creating the temporary association because the Terraform role lacks eks:AssociateAccessPolicy. No partial resource was created and CodeBuild was not started."
    },
    {
      "name": "post-failure boundary readback",
      "status": "passed",
      "result": "Read-only checks report zero associated access policies for the Vault bootstrap identity and zero builds for the Vault bootstrap CodeBuild project."
    },
    {
      "name": "scoped deploy IAM remediation",
      "status": "passed",
      "result": "The Terraform role received only AssociateAccessPolicy and DisassociateAccessPolicy on the dedicated Vault bootstrap EKS access entry, plus StartBuild, BatchGetBuilds, and ListBuildsForProject on the dedicated Vault bootstrap CodeBuild project."
    },
    {
      "name": "resumed deploy apply and emergency revocation",
      "status": "blocked_and_recovered",
      "result": "Terraform created the temporary association but then failed while reading it because eks:ListAssociatedAccessPolicies is absent. No CodeBuild build was started. The authorized administrative principal immediately disassociated the exact cluster-admin policy; post-revocation count is zero."
    }
  ],
  "notes": [
    "No secret material is admissible evidence.",
    "The current local Kubernetes context targets a separate local cluster; its namespace result is not evidence for the target EKS cluster.",
    "No Helm, CodeBuild, Vault, secret read, secret change, or deployment action occurred.",
    "The dedicated private CodeBuild project is the only available target-cluster execution path. It checks the namespace and Secret name without reading data before its Helm deployment command.",
    "Resumption requires explicit approval to add only eks:ListAssociatedAccessPolicies for the dedicated Vault bootstrap EKS access entry. It is required for Terraform to observe the temporary association and for the post-revoke verifier; it grants no mutation or cluster access.",
    "No Helm, CodeBuild, Vault, secret read, secret change, or Vault deployment action occurred."
  ]
}
