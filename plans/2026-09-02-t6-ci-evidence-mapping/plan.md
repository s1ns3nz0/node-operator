# T-6: CI evidence mapping

1. Convert compact PR/release summaries to `iac` and `kubernetes` envelopes.
2. Reject raw evidence and unsupported source fields before transport.
3. Keep delivery disabled by default and test only injected transports.
