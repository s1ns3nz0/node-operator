# Harness bootstrap

Apply repository-local model routing, task-graph validation, adapter checks,
and clean-room debrief instructions before T-8. Global skill installation is
intentionally excluded; the pinned contract is recorded in `harness/`.
