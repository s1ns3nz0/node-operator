# Cost-optimized private Hoodi architecture

1. Map fixed node-group and egress constraints in Terraform, policy, and live AWS.
2. Implement distinct scale-to-zero platform, consensus, and execution pools with isolated labels, taints, and least-privilege egress SGs.
3. Update policy and deployment contracts; validate offline plan and security tests.
4. Review, merge, apply the approved Terraform change, then validate live nodes and readiness before reattempting workloads.
