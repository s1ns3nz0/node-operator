# T-2 AWS preflight

1. Confirm currently supported EKS version and private node service dependencies.
2. Update the pinned Kubernetes version and add least-privilege VPC endpoints.
3. Add static checks and a bounded apply/destroy runbook without executing cloud actions.
4. Record evidence, independent review, and clean-room debrief.
