## Scope

Preserve the default fresh-network mode and add a separately selected existing-network mode that manages exactly six public-edge resources. Existing VPC, private subnets and private route table remain baseline-owned data references. Blocking preconditions verify the configured identities, CIDRs and availability zone; moved blocks preserve the existing fresh-mode address upgrade.

Declare an isolated S3 backend with a portable, non-secret example and migration procedure. Include the example in release bundles and execute focused contracts in CI. Preserve current main's security changes; this PR does not replace the older branch tree wholesale.

## Actual reconciliation evidence

- Imported exactly six known public-edge resources into a private local candidate.
- Independently reviewed saved plan changed only five resources' metadata tags; no create, delete, replacement or routing change.
- Post-apply refresh plan was no-drift.
- Normally migrated the verified state to the isolated encrypted foundation key under the approved role, without force-copy or resource apply.
- Remote content and all six identities matched; S3 used the approved CMK; remote refresh plan had six no-ops.
- Terraform 1.5.7 reset lineage and serial during empty-remote initialization. This is explicitly recorded, not misrepresented as preserved metadata. Private backups remain available; obsolete local apply targets are read-only.

## Verification boundaries

The backend fixture proves unconfigured S3 initialization fails and an explicit local replacement initializes and plans offline. It is not a full-module fresh-deployment claim. Existing-mode source assertions and module validation complement the private rendered-plan/import evidence; none substitutes for a real zero-resource rebuild.

No destructive rehearsal, scheduler, validator custody ceremony, broader baseline apply or ops-host replacement is included. Remaining program gates, including real human review refresh, GitOps protection entitlement and ops-state reconciliation, are kept incomplete in the task bundle.
