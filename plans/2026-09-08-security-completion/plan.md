# Complete security tasks 1–5

The active goal retains all five requirements in the contract. Item 6 (validator UC final evidence and eventual destructive zero-resource rehearsal) is not substituted for them and is not authorized here.

1. Observe the user's temporary review-rule change; keep required tests and admin enforcement. Merge PR129 normally at a verified exact head.
2. Integrate the changed main into PR127, retain all reviewed hardening, run checks and merge normally. Review/merge GitOps PR46 through its supported controls.
3. Obtain live exact-head passing, deliberately failing non-sensitive fixture, and review-refresh evidence. Make proven controls required; record any actual platform entitlement blocker rather than treating an unenforced workflow as protection.
4. Verify private target inventory, public CA mounts, immutable image compatibility and network policies; apply reviewed additive prerequisites and exercise actual passive DAST. Fix real runtime defects, not the pass criteria.
5. Establish exact existing state/resource ownership before any import, move or apply. Keep raw plans/state outside Git with private permissions. Sol reviews precise saved plans before live mutation; prevent duplicate ownership, replacements and deletion. Prove backend access remains usable and relevant final plans have no managed drift.
6. Maintain evidence per requirement, independent reviews and fresh clean-room debrief. Do not mark the goal complete while any requirement lacks authoritative proof.

DoD DEVELOP–DEPLOY, SSDF PO/PS/PW/RV and SP 800-204D 5.1.1–5.1.4 coverage is retained from the existing full red-team report; activation must be proved, not inferred from those baseline mappings.
