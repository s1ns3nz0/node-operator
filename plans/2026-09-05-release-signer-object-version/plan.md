# Signer immutable-object version read

1. Record the CodeBuild `GetObjectVersion` denial after VersionId selection.
2. Add the scoped signer permission to live IAM and Terraform with a regression assertion.
3. Validate, review, merge, and release using a new immutable tag.
