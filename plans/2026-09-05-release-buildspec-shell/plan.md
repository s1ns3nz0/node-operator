# Release signer Bash contract

1. Record the confirmed `/bin/sh` `pipefail` failure.
2. Declare Bash in the signer buildspec and add a structural regression test.
3. Validate, review, merge, and run a new protected tag release.
