{
  "task_id": "2026-09-07-hoodi-validator-lifecycle",
  "status": "in_progress",
  "observations": [
    {"source": "repository", "fact": "The existing Prysm overlay is beacon-only and the existing custody helpers are explicitly test-only."},
    {"source": "local test", "fact": "The UC-1 validator accepted one Hoodi deposit-data entry at exactly 32000000000 Gwei with matching type-1 withdrawal credentials and rejected a changed amount."},
    {"source": "official Etherscan and Beaconcha.in documentation checked 2026-09-07", "fact": "Etherscan supports Hoodi chain ID 560048. Beaconcha.in V2 uses authenticated POST requests with explicit chain hoodi; its validator endpoint returns public status, while attestation and proposal history are distinct endpoints. Explorer data is corroboration only."},
    {"source": "local test", "fact": "The external observer records a V2 Beaconcha.in not-configured result without an API token and retains only the response hash; shellcheck and harness graph validation passed."},
    {"source": "local render", "fact": "The operational signer renderer accepts only same-account ECR digests and emits Vault file injection, TLS PKCS#12 files, POSTGRES_PASSWORD_FILE, PostgreSQL password authentication, and a retained PVC. It rejects test-only identifiers and inline signer database passwords."},
    {"source": "local render", "fact": "The Prysm validator client renderer produces a zero-replica deployment with a pinned private image, private Beacon-only egress, HTTPS external signer URL, pinned signer CA, and no Vault access or service-account token mount."},
    {"source": "local static test", "fact": "The UC-5 recovery gate refuses a nonzero client or signer, a live fence holder, a missing/mismatched retained slashing DB PVC, and never activates the validator client during signer-only recovery."},
    {"source": "AWS read-only preflight 2026-09-07", "fact": "Account 106760547719 EKS cluster node-operator is ACTIVE at Kubernetes 1.35 with private endpoint access enabled and public endpoint access disabled."},
    {"source": "private SSM/EKS/Vault read-only health 2026-09-07", "fact": "Vault is initialized, unsealed, HA-enabled with Raft storage, and all three Vault Pods report Running/Ready. The validator-operations namespace exists and is Active."},
    {"source": "targeted Terraform apply and AWS read-back 2026-09-07", "fact": "The private Prysm ECR repository and its supporting KMS key were created through a reviewed plan containing three additions and no changes or destroys. The repository is KMS-encrypted, immutable, and scan-on-push; the environment-bound GitHub OIDC role has only ECR upload actions for this repository. No Prysm image has been mirrored and no validator workload was started."},
    {"source": "targeted Terraform apply and AWS read-back 2026-09-07", "fact": "The canonical validator audit bucket was created through a reviewed plan containing twelve additions and no changes or destroys. Object Lock is enabled with two-year governance retention, versioning, KMS default encryption, public-access blocks, TLS-only and encrypted-write bucket policy, and 90-day/365-day storage transitions. No workload log has been delivered yet."},
    {"source": "AWS and GitHub read-back 2026-09-07", "fact": "Private S3, CloudWatch Logs, and STS endpoints are available. The GitHub validator-client-ecr-mirror Environment exists and is limited to protected branches. Environment variables could not be written with the available GitHub credential."},
    {"source": "Terraform safety preflight 2026-09-07", "fact": "Pod Identity association apply was not performed: the generated plan also proposed five private endpoint deletions, node-group scaling changes, and existing KMS policy changes due to state/configuration drift. The plan was rejected before apply."},
    {"source": "Terraform state and Git history analysis 2026-09-07", "fact": "The active state contains the September 5 Hoodi consensus/execution node pools and supporting network resources, while this task branch is based on the September 4 main baseline. The later zero-resource Hoodi release foundation provides separate bootstrap and ops-access roots; it must be integrated deliberately rather than treating active Hoodi resources as drift to delete."},
    {"source": "integration verification 2026-09-07", "fact": "The zero-resource Hoodi release foundation was merged into the lifecycle branch. Terraform validate, zero-resource and release-bootstrap contracts, validator runtime rendering, and harness:check passed. The legacy SSM resource addresses remain only for reviewed migration to the separate ops-access root; no state migration or destructive reconciliation was applied."},
    {"source": "AWS read-only plus local validation 2026-09-07", "fact": "The live private EKS operations host i-02c57d75e7f6810b1 is running in the active VPC with Terraform ops-access tags and uses shared baseline SSM interface endpoints. The isolated ops-access root now requires an explicit S3 backend config and has a migration mode that references those shared endpoints without taking ownership. Terraform validate and the release, zero-resource, runtime, and harness contracts passed. No operations-access plan or apply was run against AWS."},
    {"source": "targeted Terraform apply, AWS and GitHub read-back 2026-09-07", "fact": "The Fluent Bit collector private ECR mirror foundation was created through a reviewed target plan with four additions and no changes or destroys: a KMS-rotated immutable scan-on-push repository, and a GitHub Environment-bound OIDC role with upload-only permissions scoped to that repository. The validator-log-collector-ecr-mirror GitHub Environment is protected-branches-only and contains only the account ID and role ARN. The repository has no images and no collector workload was created."},
    {"source": "official Fluent Bit registry and local container dry-run 2026-09-07", "fact": "Fluent Bit 4.2.3 was selected from the official stable release line and its official multi-platform source manifest was resolved as cr.fluentbit.io/fluent/fluent-bit@sha256:a5761fa961cb22dd0875883a4d446b1acd99d4935d77358aa9f50ee177e44fe2. The exact image accepted the rendered collector fluent-bit.conf in --dry-run mode. Mirroring is intentionally blocked until this workflow reaches a protected branch: the task branch is not protected while the GitHub Environment permits protected branches only."},
    {"source": "shellcheck", "fact": "The two new operational scripts passed ShellCheck."},
    {"source": "harness", "fact": "harness:check passed. harness:verify reaches known repository fixture failures and an unavailable OSV fixture before completion; these failures are outside this task's changed files."},
    {"source": "local test", "fact": "The evidence-envelope validator accepted a public lifecycle record and rejected a forbidden token field."},
    {"source": "terraform", "fact": "The pinned AWS provider was installed in backend-free mode and terraform validate passed for the complete configuration. An offline plan remains blocked by the repository's S3 backend initialization contract."},
    {"source": "local", "fact": "The private Beacon and external-explorer observer scripts passed ShellCheck; runtime observation remains pending a real fresh validator public key and private EKS access."},
    {"source": "local", "fact": "The isolated Fluent Bit overlay rendered successfully, rejects a non-private/non-digest image, and passes its structural contract test."},
    {"source": "terraform", "fact": "The CloudWatch Logs to Kinesis Firehose to Object Lock S3 delivery resources were added and terraform validate passed."},
    {"source": "local", "fact": "Vault audit verifier and values contract passed ShellCheck; private runtime verification is pending a configured administrator session and socket collector."},
    {"source": "local", "fact": "The derived OpenSearch bulk builder rejects forbidden evidence and generated paired bulk metadata/documents from a schema-valid record."},
    {"source": "targeted Terraform apply and private EKS/AWS read-back 2026-09-07", "fact": "The private Fluent Bit collector runs Ready on all three eligible nodes through an EKS Pod Identity association. Its workload-scoped role writes only the validator-workloads and validator-security CloudWatch log groups. Live Nethermind and Prysm records reached the workload group and Vault audit records reached the security group; no collector authorization errors remained after rollout."},
    {"source": "targeted Terraform apply and AWS read-back 2026-09-07", "fact": "Both validator CloudWatch groups are subscribed to the active validator Firehose delivery stream. A live compressed record was delivered to the canonical validator prefix in the existing Object Lock audit bucket with SSE-KMS and governance retention through 2028-09-07. The Firehose error path includes the mandatory error-output-type token and is kept separate from canonical records."},
    {"source": "private EKS read-only inspection and local contract test 2026-09-07", "fact": "A legacy base-wide remote-signer Service selected the test-only signer endpoint because its selector omitted node-operator.io/validator-set; the similarly base-wide fence Lease was also present. No operational signer was started. The remediation removes both base-wide boundary objects, retains only renderer-created set-scoped Service and Lease resources, and makes readiness reject any legacy or unscoped signer Service."},
    {"source": "local public deposit-data inspection 2026-09-07", "fact": "The verified ethstaker deposit CLI emits standard SSZ byte fields without a 0x prefix. The UC-1 validator must normalize exactly valid unprefixed or prefixed hexadecimal fields before writing its explicitly 0x-prefixed public evidence, and reject malformed values."},
    {"source": "private EKS read-only inspection 2026-09-07", "fact": "The live Prysm Beacon API is reachable only through its Ready StatefulSet Pod because no prysm-beacon Service exists. Private UC-3 and UC-4 observers must select that Pod only when exactly one Ready label-matched Pod exists, and otherwise fail closed."},
    {"source": "live macOS lifecycle evidence collection 2026-09-07", "fact": "The collector wrote its redacted JSON evidence successfully but then hit the Bash 4-only phase uppercase expansion in its status line. The portable collector must use tr and verify that no Bash 4-only expansion remains."},
    {"source": "local evidence snapshot 2026-09-08", "fact": "UC-2 evidence was recorded at `.evidence/hoodi-001/uc-2/uc-2-hoodi-001-20260908T004449Z.json`; it shows signer + slashing DB pods Ready, signer lease holder fixed, and Vault-backed runtime identity intended for bounded signing-path onboarding."},
    {"source": "live evidence bundle 2026-09-10", "fact": "UC-1 public attestation/deposit evidence and UC-2 through UC-5 live evidence indicate validator-public-key continuity with chain state (`validator_index:1559065`) and no secrets in evidence payloads. See `plans/2026-09-10-hoodi-uc-live-verification/evidence.json` for the summary."}
  ],
  "pending_gates": ["activation queue", "first duty"]
}
