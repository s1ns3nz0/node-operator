apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: insecure-fixture
spec:
  template:
    spec:
      containers:
        - name: node
          image: registry.example/node:latest
          securityContext:
            privileged: true
            runAsNonRoot: false
            readOnlyRootFilesystem: false
